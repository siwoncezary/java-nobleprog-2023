# Zadanie 
Otwórz klasę `Person` w pakiecie `data` i uzupełnij definicje pól o modyfikatory:
- pole `firstName` dostępne dla wszystkich klas i pakietów
- pole `visits` dostępne tylko wewnątrz klasy
- pole `isActive` dostępne wewnątrz klasy i klas potomnych